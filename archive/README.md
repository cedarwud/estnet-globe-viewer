# Archive

這裡只放已不再是 live authority 的文件，例如：

- 被取代的 phase 計畫
- 過時的設計稿
- 已封存的中間版本文件

規則：

1. live SDD、README、phase 記錄、devlog 不要直接堆在這裡。
2. 文件一旦失去 authority 身分，就應移到 `archive/`，而不是繼續混在 `docs/`。
3. workspace-internal scratch 仍應留在工作區其他路徑，不要假借 `archive/` 名義放進交付 repo。
