# Earth Asset Helper Scripts

Place reproducible Earth asset preprocessing helpers in this directory.

Step 0 does not add a real preprocessing script because no Earth runtime texture is being committed yet. When Step 1 starts, prefer a small documented helper here over ad hoc one-off commands that are not recorded in `docs/assets/earth-assets.md`.
